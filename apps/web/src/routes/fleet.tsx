// Stand-in for Task 17, which replaces this wholesale with the Fleet page
// (node cards, memory bars, pairing-code dialog). The router below imports
// this so it can wire up the `/` route today.
export function FleetRoute() {
  return null;
}
