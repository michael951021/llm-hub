{
  "name": "@modelhub/proto",
  "version": "0.0.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate": "cd ../../proto && pnpm exec buf generate",
    "lint": "cd ../../proto && pnpm exec buf lint",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.2.0",
    "@connectrpc/connect": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
